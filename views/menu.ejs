<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
</head>
<body>
    <h1>Full Menu</h1>
    <ul>
       <% RESTAURANT.menu.forEach(item => { %>
        
        <h3><%= item.name %> PRICE: $<%= item.price %> RATING: <%= item.rating %> DETAILS: <%= item.details %></h3>
       
       <% }) %>
       
        
        
    </ul>
</body>
</html>